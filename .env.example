# Copy this file to `.env` and replace the value with your Google Vision API key
VITE_GOOGLE_API_KEY=your_api_key_here
VITE_GOOGLE_MAPS_API_KEY=your_maps_api_key_here

# Enable client-side generative features (verification questions + image descriptions)
# Set to `false` to disable client-side generation (recommended for production — prefer server-side generation)
VITE_ENABLE_GENERATIVE_QS=true
